---
// SectionDivider.astro - Horizontal divider between sections

interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`border-t border-mistral-border my-16 ${className}`} role="separator" aria-hidden="true" />