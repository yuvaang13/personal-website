---
// SectionDivider.astro - Horizontal divider between sections

interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`border-t border-mistral-text my-8 ${className}`} role="separator" aria-hidden="true" />