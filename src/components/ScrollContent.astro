---
// ScrollContent.astro - Wrapper for scroll-animated content sections

interface Props {
  class?: string;
}

const { class: className = '', children } = Astro.props;
---

<div class={`relative ${className}`}>
  <slot />
</div>